apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "k8s-eni-tagger.fullname" . }}
  labels:
    {{- include "k8s-eni-tagger.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "k8s-eni-tagger.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      annotations:
        {{- with .Values.podAnnotations }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      labels:
        {{- include "k8s-eni-tagger.selectorLabels" . | nindent 8 }}
        {{- with .Values.podLabels }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "k8s-eni-tagger.serviceAccountName" . }}
      {{- if not (kindIs "invalid" .Values.serviceAccount.automountServiceAccountToken) }}
      automountServiceAccountToken: {{ .Values.serviceAccount.automountServiceAccountToken }}
      {{- end }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
        - name: {{ .Chart.Name }}
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          args:
            - --annotation-key={{ .Values.config.annotationKey }}
            - --max-concurrent-reconciles={{ .Values.config.maxConcurrentReconciles }}
            - --metrics-bind-address={{ .Values.config.metricsBindAddress }}
            - --health-probe-bind-address={{ .Values.config.healthProbeBindAddress }}
            {{- if .Values.config.watchNamespace }}
            - --watch-namespace={{ .Values.config.watchNamespace }}
            {{- end }}
            {{- if .Values.config.dryRun }}
            - --dry-run
            {{- end }}
            {{- if eq (include "k8s-eni-tagger.leaderElectionEnabled" .) "true" }}
            - --leader-elect
            {{- end }}
            {{- if .Values.config.subnetIDs }}
            - --subnet-ids={{ .Values.config.subnetIDs }}
            {{- end }}
            {{- if .Values.config.allowSharedENITagging }}
            - --allow-shared-eni-tagging
            {{- end }}
            {{- if not .Values.config.enableENICache }}
            - --enable-eni-cache=false
            {{- end }}
            {{- if .Values.config.enableCacheConfigMap }}
            - --enable-cache-configmap
            {{- end }}
            - --aws-rate-limit-qps={{ .Values.config.awsRateLimitQPS }}
            - --aws-rate-limit-burst={{ .Values.config.awsRateLimitBurst }}
            - --pprof-bind-address={{ .Values.config.pprofBindAddress }}
          env:
            {{- range $key, $value := .Values.env }}
            - name: {{ $key }}
              value: {{ $value | quote }}
            {{- end }}
          {{- with .Values.envFrom }}
          envFrom:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          ports:
            - name: metrics
              containerPort: {{ .Values.health.port | default 8090 }}
              protocol: TCP
            - name: health
              containerPort: {{ .Values.health.port | default 8081 }}
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /healthz
              port: health
            initialDelaySeconds: 15
            periodSeconds: 20
          readinessProbe:
            httpGet:
              path: /readyz
              port: health
            initialDelaySeconds: 5
            periodSeconds: 10
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
          {{- with .Values.extraVolumeMounts }}
          volumeMounts:
            {{- toYaml . | nindent 12 }}
          {{- end }}
      {{- with .Values.extraVolumes }}
      volumes:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
